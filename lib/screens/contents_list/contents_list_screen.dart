import 'package:flutter/material.dart';

class ContentsListScreen extends StatelessWidget {
  const ContentsListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('ContentsListScreen'),
      ),
    );
  }
}
